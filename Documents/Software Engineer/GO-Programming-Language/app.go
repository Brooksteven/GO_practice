package main

import "fmt"

func main() {
	//here we are calling a function "Print". Print is a function/command that we are calling is outputing "Hello World". You run fmt which calling print and outputting "Hello World" by running in the terminal "go run app.go"
	fmt.Print("Hello World")
}